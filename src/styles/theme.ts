import { createTheme } from '@material-ui/core'

export const standardTheme = createTheme({
  palette: {
    primary: {
      main: '#00aa4a'
    },
    background: {
      default: '#ededed'
    },
    success: {
      main: '#16C172'
    }
  }
})
